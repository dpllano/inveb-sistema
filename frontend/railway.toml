# Railway configuration for INVEB Frontend
[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
# Serve static files with serve package
startCommand = "npx serve dist -s -l $PORT"
